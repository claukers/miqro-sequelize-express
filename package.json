{
  "name": "@miqro/modelhandlers",
  "version": "0.4.13",
  "description": "",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/claukers/miqro-sequelize-express.git"
  },
  "scripts": {
    "predoc": "rm -Rf docs;",
    "doc": "typedoc --mode library --plugin typedoc-plugin-markdown --out docs src",
    "prepare": "npm run build",
    "prebuild": "rm -Rf dist;",
    "build": "tsc",
    "test": "NODE_ENV=test nyc mocha test/*.test.ts",
    "testfile": "NODE_ENV=test nyc mocha",
    "lint": "tslint -c ./tslint.json -p ./tsconfig.json",
    "fixlint": "tslint --fix -c ./tslint.json -p ./tsconfig.json"
  },
  "author": "claukers",
  "license": "ISC",
  "dependencies": {
    "@miqro/core": "^0.4.0",
    "@miqro/database": "^0.2.11",
    "@miqro/handlers": "^0.5.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.0",
    "nyc": "^15.0.0",
    "sinon": "^9.0.1",
    "supertest": "^4.0.2",
    "ts-node": "^8.6.2",
    "tslint": "^6.1.0",
    "typedoc": "^0.17.0",
    "typedoc-plugin-markdown": "^2.2.17",
    "typescript": "^3.8.3"
  },
  "engines": {
    "node": ">=10.0.0",
    "npm": ">=6.0.0"
  }
}
